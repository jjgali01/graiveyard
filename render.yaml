services:
  - type: web
    name: graiveyard
    runtime: node
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: graiveyard-db
          property: connectionString

databases:
  - name: graiveyard-db
    plan: free
